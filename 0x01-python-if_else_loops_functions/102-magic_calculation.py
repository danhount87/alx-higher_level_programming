#!/usr/bin/python3
# zinzinpolice


def magic_calculation(a, b, c):
    """Python function that does exactly the same as
    the following Python bytecode:"""
    if a < b:
        return (c)
    if c > b:
        return (a + b)
